<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.accounting.mapper.AccountingSubjectMapper">

    <select id="selectByParentId" resultType="com.accounting.entity.AccountingSubject">
        SELECT * FROM accounting_subject 
        WHERE parent_id = #{parentId}
        ORDER BY sort ASC
    </select>
    
    <select id="selectByType" resultType="com.accounting.entity.AccountingSubject">
        SELECT * FROM accounting_subject 
        WHERE type = #{type}
        ORDER BY sort ASC
    </select>
    
    <select id="selectEnabledLeafSubjects" resultType="com.accounting.entity.AccountingSubject">
        SELECT * FROM accounting_subject 
        WHERE is_leaf = 1 AND enabled = 1
        ORDER BY code ASC
    </select>
    
</mapper>